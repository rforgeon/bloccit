<h1>Edit Question</h1>

 <div class="row">
   <div class="col-md-4">
     <p>Guidelines for Questions</p>
     <ul>
       <li>No dumb questions.</li>
       <li>No question is a dumb question.</li>
       <li>No profanity.</li>
     </ul>
   </div>
   <div class="col-md-8">
     <%= form_for @question do |f| %>
       <div class="form-group">
         <%= f.label :title %>
         <%= f.text_field :title, class: 'form-control', placeholder: "Enter question title" %>
       </div>
       <div class="form-group">
         <%= f.label :body %>
         <%= f.text_area :body, rows: 8, class: 'form-control', placeholder: "Enter question body" %>
       </div>
       <div class="field form-group">
         <div class="checkbox-inline">
           <%= f.check_box :resolved %>
           <%= f.label :resolved, "The question is resolved." %><br>
         </div>
       </div>
       <div class="form-group">
         <%= f.submit "Save", class: 'btn btn-success' %>
       </div>
     <% end %>
   </div>
 </div>
